Golden fixtures for snapshot-backed workflow regression tests.

Rules:
- Compare normalized JSON only.
- Dynamic absolute paths are replaced with `<snapshot-dir>`.
- Update these files only when the contract change is intentional.
