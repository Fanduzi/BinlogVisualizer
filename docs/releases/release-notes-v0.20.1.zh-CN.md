# BinlogViz v0.20.1 发布说明

发布日期：2026-04-21

## Bug 修复

- **Probe 偏移解析容错**：`analyze` 命令在探测 binlog 文件时，若偏移量落在非事件边界（如损坏或截断的事件），不再崩溃。最后时间戳现在会优雅地回退到首个事件时间戳，而非返回致命错误。

## 改进

- **下钻图表 Y 轴标签**：HTML 报告中的表级活动图和操作分布图现在显示 "每分钟行数" Y 轴标签，提升可读性。

## 破坏性变更

无。
