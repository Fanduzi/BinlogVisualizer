# BinlogViz v0.10.1 Release Notes

Release date: 2026-04-09

## Scope

`v0.10.1` is a patch release on top of `v0.10.0`. It does not change analysis behavior; it aligns the public documentation and install guidance with the already shipped pattern-aware compare and trend workflows.

## Highlights

- updated README install and download examples to point at the current release version
- aligned README workflow descriptions with the shipped `pattern_changes` and `pattern_trends` capabilities
- updated the landing page release narrative so the current release badge and timeline no longer stop at `v0.10.0`
- kept CLI reference and install guidance consistent with the published release surface

## Compatibility Notes

- no CLI flags, report contracts, or analysis semantics changed in this patch release
- release assets, checksums, and install examples now consistently reference `v0.10.1`

## Breaking Changes

None.
